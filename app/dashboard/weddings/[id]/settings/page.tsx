export default function SettingsPlaceholder() {
  return (
    <div className="space-y-4">
      <h1 className="text-lg font-semibold">Settings</h1>
      <p className="text-foreground/70">Subdomain (slug) and release. (Form coming in a later task.)</p>
    </div>
  );
}
